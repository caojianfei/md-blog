
### 🎨 核心视觉规范（全局基础）

在开始具体页面前，这是你博客的“设计灵魂”。

| 维度 | **浅色模式 (Light Mode)** | **深色模式 (Dark Mode)** |
| --- | --- | --- |
| **背景色** | `#FFFFFF` (纯净白) | `#0D1117` (GitHub 深) 或 `#050505` (极简黑) |
| **主要文字** | `#1A1A1A` (高对比) | `#E6EDF3` (柔和白) |
| **次要文字** | `#666666` | `#8B949E` |
| **边框/分割线** | `#E1E4E8` (细线) | `#30363D` (深灰线条) |
| **品牌点缀色** | 建议：`#007AFF` (经典蓝) 或 `#2EA043` (Git 绿) | 同左，但需微调亮度以符合对比度要求 |

---

### 📝 AI 提示词整理（直接复用版）

你可以直接将以下提示词发给 AI 助手，引导它为你生成 UI 方案或代码。

#### 1. 全局设计风格定义 (Global Style Guide)

> **提示词：**
> “请为我设计一套程序员博客的 UI 系统。风格要求：**High-end Minimalist Tech Aesthetics**。
> **关键元素：**
> * **字体：** 正文使用无衬线体 Inter，元数据和代码块使用等宽字体 JetBrains Mono。
> * **质感：** 减少阴影，大量使用 1px 的边框（Border）来区分区域，模拟代码编辑器的层级感。
> * **模式：** 必须包含完美的深色（Dark）和浅色（Light）模式切换逻辑。
> * **交互：** 所有的 Transition 应该是 0.2s 的 Ease-in-out，体现丝滑感。”
> 
> 

#### 2. 博客首页：技术感与“系统”入口 (Home Page)

> **提示词：**
> “设计一个博客首页。
> **布局要求：**
> * **Hero Section：** 极简左对齐标题，右侧是一个抽象的 ASCII 艺术或动态的 Git 提交网格小组件。
> * **文章列表：** 采用‘流式列表’。左侧是 Monospace 字体的日期，中间是加粗标题，右侧是简单的标签。鼠标悬浮时，整行出现淡淡的背景高亮（类似 IDE 的行选中状态）。
> * **高级感插件：** 在侧边栏展示一个‘Current Stack’模块，用简洁的图标显示当前的开发环境版本号。”
> 
> 

#### 3. 详情页：极致阅读与代码展示 (Post Detail)

> **提示词：**
> “设计博客文章详情页。
> **核心逻辑：**
> * **排版：** 居中单栏，最大宽度 760px。行间距 1.75。
> * **代码块：** 完全复刻 GitHub Dark/Light 风格，具备语言标识标签、行号和点击复制功能。
> * **Markdown 增强：** 表格需具备响应式滚动；引用块（Blockquote）左侧有 4px 的点缀色垂直条；图片具有 1px 的边框和微弱的圆角。
> * **导航：** 右侧有一个半透明的目录（TOC），支持滚动监听，当前章节自动加粗。”
> 
> 

#### 4. 关于我：程序员的数字简历 (About Page)

> **提示词：**
> “设计‘关于我’页面。不要做成传统的简历，要像一个 **Dashboard**。
> **模块包含：**
> * **Tech Radar：** 用简单的网格展示技术栈。
> * **GitHub Stats：** 一个模拟 GitHub Profile 贡献图的区域。
> * **Project Mirror：** 即使不展示完整项目，也用卡片展示 2-3 个核心仓库的 Star 数和语言占比。
> * **Connect：** 底部提供 SSH Key 风格的联系方式展示区。”
> 
> 

---

### 💡 关键设计思路总结

为了让你的博客不落俗套，请坚持以下 **“三个不”** 原则：

1. **不要使用大圆角：** 程序员的审美通常更偏向 **“硬朗”**。建议圆角控制在 `4px - 6px` 之间，甚至使用完全的直角。
2. **不要使用复杂的渐变：** 尽量使用纯色平涂或非常微弱的线性渐变。高级感来自于对 **“线条”** 的控制，而非色彩的堆砌。
3. **不要忽略 Loading 状态：** 设计一个类似终端加载字符（`/ - \ |`）或进度条（NProgress）的加载动效，这种细节最能体现“程序员自研”的身份。
